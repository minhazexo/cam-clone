# RScan — Python runtime (web server + CLI scanner)
# Install:  pip install -r requirements.txt
#
# Headless OpenCV is intentional: Vercel/containers have no libGL, and this
# repo never calls imshow/waitKey, so opencv-python-headless is a drop-in.

flask>=3.0
opencv-python-headless>=4.8
numpy>=1.24
pymupdf>=1.28.2
pillow>=10
