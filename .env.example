# RScan environment variables
# Copy to .env and adjust as needed. Defaults work without a .env file.

# HTTP port for `python app.py` (Flask)
PORT=5000

# Set to 1 for verbose Flask / OpenCV logs
RSCAN_DEBUG=0
